# Examples

This folder contains runnable examples.

Purpose:

- show how to use the active instrument APIs
- keep bench-use example files out of the repo root
- group examples by instrument or by clear use case

Rule:

- reusable library code goes in `source`
- runnable example files go in `examples`

Current layout:

- `hp_4192a/`: examples for the HP 4192A
