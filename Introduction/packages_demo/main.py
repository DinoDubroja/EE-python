import package1.yapper as yapper
import package2.math as math
import package1.printer as printer

nums = [1, 2, 3, 4, 5]
avg = math.average(nums)

random_string = yapper.yap(10)
printer.printUpperCase(random_string)